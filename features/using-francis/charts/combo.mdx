---
title: "Combo chart"
description: "Mixes columns and a line over time to compare measures of different scale or unit on one chart."
icon: "chart-mixed"
---

{/* <video controls className="w-full aspect-video rounded-xl" poster="/images/features/charts/combo.jpg" src="/videos/features/charts/combo.mp4" /> */}

A combo chart plots values over time using more than one representation, typically columns for one measure and a line for another. Use it when you want to compare measures of different scale or unit on a single chart, such as revenue as columns against gross margin percentage as a line. Because the two measures rarely share a scale, a combo chart carries two y-axes.

## Settings

Click a chart to open its settings, grouped in the sidebar. Remove a group with the **X** next to its name to hide that element from the chart, and use **+** to add a series or a target.

### Chart

<AccordionGroup>
  <Accordion title="Date range">
    The span of data the chart shows. Choose a dynamic range (Last month, YTD, LTM, Full year, Last year, or Next year) or set a Custom range. Dynamic ranges resolve against the Last close date set for the model, so they roll forward automatically as you close each period.
  </Accordion>
  <Accordion title="Group by">
    How periods group along the x-axis: monthly, quarterly, calendar year, fiscal year, or all.
  </Accordion>
  <Accordion title="Colors">
    The color palette applied to the series. Options are Ocean blue, Olive green, Sun burst, Death star, Pastel, Nature, Temperature, Dawn, Blue, Green, Orange, Red, Pink, Purple, and Gray. Override the palette on an individual series from the series settings.
  </Accordion>
  <Accordion title="Line style">
    Whether the line is Smooth or Straight.
  </Accordion>
</AccordionGroup>

### Series

The series shown in the chart, each drawn from a row, group, or calculation in your model. For each series you can include multiple sources, such as actuals, budget, or forecast. Series order sets the legend order: the top series shows first, then the next, and so on. Sources are [styled automatically](/features/using-francis/charts/overview#actuals-and-forecast-styling): actuals render in the full series color, and budget or forecast render semi-transparent as columns and dashed as a line.

Click a series to reveal its own settings. When a series has a single source, click the series directly. When it has multiple sources, click the specific source to open its settings.

<AccordionGroup>
  <Accordion title="Type">
    Whether the series renders as a column or a line. This also fixes the axis: columns always plot against the primary y-axis and lines always against the secondary. To move a measure to the other axis, change its type.
  </Accordion>
  <Accordion title="Title">
    The series name. Defaults to the name of the row, group, or calculation the series is drawn from; override it to rename the series.
  </Accordion>
  <Accordion title="Color">
    Override the palette color assigned to this series.
  </Accordion>
  <Accordion title="Data labels">
    Show or hide the value labels on the series.
  </Accordion>
  <Accordion title="Absolute values">
    Plot the absolute value of each point, ignoring sign.
  </Accordion>
</AccordionGroup>

### Title

<AccordionGroup>
  <Accordion title="Content">
    The chart title. Defaults to the name of the row, group, or calculation the chart is drawn from; override it with free text.
  </Accordion>
  <Accordion title="Align">
    Position the title to the left, center, or right.
  </Accordion>
</AccordionGroup>

### Legend

<AccordionGroup>
  <Accordion title="Position">
    Where the legend sits. Top is the only position available for this chart type.
  </Accordion>
</AccordionGroup>

### X-axis

<AccordionGroup>
  <Accordion title="Line">
    Show or hide the x-axis line.
  </Accordion>
  <Accordion title="Slant">
    Rotate the x-axis labels by 0, 15, 30, 45, 60, 75, or 90 degrees.
  </Accordion>
</AccordionGroup>

### Primary y-axis

The left-hand axis, scaled to the measure shown as columns.

<AccordionGroup>
  <Accordion title="Title">
    The axis title, as free text.
  </Accordion>
  <Accordion title="Line">
    Show or hide the axis line.
  </Accordion>
  <Accordion title="Unit">
    Auto inherits the unit from the series in the model, or override it with number, percentage, or a currency (DKK, EUR, USD, or GBP).
  </Accordion>
  <Accordion title="Limit">
    By default the axis fits the bounds of the series. Set a fixed minimum and maximum to lock the scale.
  </Accordion>
</AccordionGroup>

### Secondary y-axis

The right-hand axis, scaled to the measure shown as a line. Same settings as the primary axis, applied independently, which is what lets a percentage sit alongside a currency amount without flattening either.

<AccordionGroup>
  <Accordion title="Title">
    The axis title, as free text.
  </Accordion>
  <Accordion title="Line">
    Show or hide the axis line.
  </Accordion>
  <Accordion title="Unit">
    Auto inherits the unit from the series in the model, or override it with number, percentage, or a currency (DKK, EUR, USD, or GBP).
  </Accordion>
  <Accordion title="Limit">
    By default the axis fits the bounds of the series. Set a fixed minimum and maximum to lock the scale.
  </Accordion>
</AccordionGroup>

### Target

<AccordionGroup>
  <Accordion title="Label">
    The target's name.
  </Accordion>
  <Accordion title="Value">
    The value the line sits at.
  </Accordion>
  <Accordion title="Color">
    The color of the line.
  </Accordion>
</AccordionGroup>

## See in action

<Note>
  See charts in practice in the [Management report](/masterclasses/reporting/management-report) masterclass.
</Note>
